# Orchestrator ledger — agent-harness

Append-only, one short paragraph per significant event. Continuity for later companion/orchestrator sessions.

## 2026-06-06 — dev-companion (spike kickoff)

Took the chief handoff: spike backlog AGENT-1..9, build via attended companion. Environment was bare — no bun/opencode/litellm; `uv` 0.11.16 present, used for the venv (CPython 3.12.13). Researched current models from the live OpenRouter catalog (my training-era picks were stale): builder=`qwen/qwen3-coder-30b-a3b-instruct`, builder-alt=`deepseek/deepseek-v4-flash` (G3 A/B), reviewer=`anthropic/claude-sonnet-4.6`; LiteLLM pinned 1.87.1; $25 cap. **AGENT-1** built inline on branch `agent-1-litellm-gateway`: `config/litellm.yaml` (3 routes), runbook in `config/README.md`, `scripts/verify-gateway.sh`. Hard $25 cap enforced at the OpenRouter key (DB-less gateway for the spike; per-request cost still logged); persistent dashboard deferred to a later DB add. AGENT-1 verified live (all 3 routes return completions; per-request cost present). Finding: `deepseek-v4-flash` is a reasoning model — reasoning-token tax matters for AGENT-9 cost accounting; verify script bumped to max_tokens 256 (PR #11).

## 2026-06-06 — dev-companion (AGENT-3)

Installed `opencode-ai@1.16.2` + `bun@1.3.14` via npm (owner-run; `npm i -g` is outside the allowlist). Wrote `opencode.json`: LiteLLM as `@ai-sdk/openai-compatible` provider (`baseURL` localhost:4000, apiKey via `{env:LITELLM_MASTER_KEY}`), three routes mapped with manual token limits (no auto-detect for custom providers), default model `litellm/builder`. Verified: `opencode run` returned OK on the builder route and the gateway logged `POST /v1/chat/completions 200`; `opencode serve` up, `/doc` returns OpenAPI. **API finding for AGENT-7/8:** the survey's `prompt_async` doesn't exist as named — actual endpoints are `/session/{id}/prompt`, `/session/{id}/wait`, `/session/{id}/children`, and `/experimental/session/{id}/background` (the experimental fan-out for G1). The wake/dispatch driver should target those.

## 2026-06-06 — dev-companion (AGENT-4)

Defined `feature-builder` (→ litellm/builder) and `reviewer` (→ litellm/reviewer) as subagents via the `agent` key in opencode.json, with prompt bodies in `agents/*.md` and safe-intersection permission sets (builder: edit allow + scoped git/gh/bun bash, deny rm-rf/sudo; reviewer: edit deny + read-only git/gh). Confirmed: both load as subagents (`opencode agent list`), config valid, and the permission rules merge into the resolved config (git */gh */bun */rm -rf */sudo */edit all present). **Verification limit (honest):** OpenCode only spawns subagents from a primary (`opencode run --agent <subagent>` falls back to default), so the two live behavioral ACs — per-agent route in gateway logs, and a permission deny actually firing — can't be exercised standalone. They fold into AGENT-6 (fan-out) and AGENT-7 (substrate dispatch), where a primary spawns these subagents for real.

## 2026-06-07 — dev-companion (AGENT-6, gate G1)

Built the fan-out stress harness as the substrate seed: `src/opencode/client.ts` (typed OpenCode REST client — createSession/sendMessage) + `src/fanout-stress.ts` (`bun run fanout`). Made it a real installable project (bun install, @types/bun, bun.lock, typecheck green). **G1 PASS:** 5-wide fan-out on 3 distinct LiteLLM routes, 3 rounds, all 15 calls OK, `wall ≈ max(task)` every round = true concurrency. Recorded in `docs/spike-results.md`. **Findings:** (1) concurrency holds via the DIRECT session API, so we sidestep OpenCode's experimental task-tool background path — corrects the survey's G1 framing; (2) `POST /session/{id}/prompt_async` DOES exist (my AGENT-3 ledger note was from an incomplete grep — synchronous `/session/{id}/message` blocks until reply and is what the harness uses); (3) caught my own flawed concurrency metric (sum/wall ratio false-negatived a high-variance round) and fixed it to wall≈max before declaring PASS. AGENT-7/8 can build dispatch on the direct-session API with confidence.

## 2026-06-07 — dev-companion (AGENT-7, build leg + a pivotal model finding)

Built the dispatch+build leg: `src/opencode/serve.ts` (spawn serve bound to a worktree via cwd) + `src/dispatch/build-leg.ts` (worktree → dispatch builder over HTTP → commit/push/PR), client gains an `agent` field. Verified end-to-end → PR #23 (`requireEnv` helper), clean mergeable code. **PIVOTAL:** qwen3-coder-30b (my original primary cheap builder) does NOT tool-call through OpenCode+LiteLLM — emits tool calls as text, never executes them. deepseek-v4-flash tool-calls natively. Swapped `builder` route qwen→deepseek (qwen kept as reference alt). This is the A/B (builder-alt) paying off exactly as designed, and a strong early G3 signal (cheap model → mergeable code). Also: builder/reviewer → `mode: all` (substrate dispatches directly, per G1). Follow-up for AGENT-9: `bun install` in the worktree so the builder can self-typecheck/test (it flagged the missing-deps `process` error itself). Next: AGENT-8 (review + wake leg) reviews PR #23 via the reviewer route + prompt_async wake (G4).

## 2026-06-07 — dev-companion (AGENT-8, review + wake leg, gate G4)

Built the review leg: client gains `promptAsync` + `waitForReply` (poll-for-idle); `src/dispatch/review-leg.ts` checks out the PR branch in a worktree, dispatches the reviewer (strong route) via the token-free wake (prompt_async fire + external idle detection), captures + posts the review. **G4 PASS** against PR #23: idle detected ~36s, review posted as a comment. The reviewer (sonnet) gave a sharp, specific review and caught the TEST-1 non-ticket — clean demo of cheap-builds/strong-reviews. Mechanism: `/api/.../wait` is 503 (unimplemented) so idle detection is message-polling (`/event` SSE is the future path); prompt_async = 204. The full loop (dispatch→build→PR→wake→review) is now proven end-to-end. Next: AGENT-9 — formal G3 verdict (mergeability + cost-per-PR), with the worktree `bun install` quality fix.

## 2026-06-07 — dev-companion (AGENT-9, make-or-break verdict — THESIS PROVEN)

Built the full-loop orchestrator (`src/dispatch/loop.ts`) + cost model (`src/dispatch/cost.ts`) + token capture (`client.sessionTokens`, legs return route+tokens), and added `bun install` to the build worktree so the builder self-verifies. Ran one real issue (parseRoutes util + tests) through dispatch→build→PR→wake→review → **PR #26**. **G3 PASS both ways:** mergeable (deepseek wrote clean typed util + bun:test, 5/5 pass) AND cost $0.078/PR (build $0.004 deepseek + review $0.074 sonnet) vs $2.80 target — ~36× under, ~$7/mo at 90 PRs vs the $250 ceiling. Per scope/final.md decision rule (G1+G3+G4 all pass): **thesis proven, greenlight the full port.** Loop runs on the open stack; both drivers (lock-in, cost) delivered. Remaining: G5 (AGENT-2 Linear MCP) + G6 (AGENT-5 remote attach), parallel/non-gating; security envelope (ADR 0007) gates unattended+deploy. Builder-output PRs #23 (requireEnv) + #26 (parseRoutes) are real mergeable utils.

## 2026-06-07 — dev-companion (heavy cost test — calibrating the verdict)

Owner pushed back (rightly): the G3 tests were trivial; cost on toy work doesn't prove the thesis for reasoning-heavy PRs, and #26 had missing trailing newlines (I'd over-called "mergeable"). Fixed #26's newlines. Ran a HEAVY test: a SQLite dispatch registry (bun:sqlite, 5-state machine + transactions + idempotent resume + 13 tests) — PR #28. Result: the cheap builder (deepseek) produced a strong first draft (correct core, 13/0 tests pass, typecheck clean, WAL added unprompted) but NOT one-shot-mergeable — the sonnet review caught 6 findings incl 2 High (setSessions atomicity, hardcoded states). Cost $0.090/PR even heavy. **Calibrated verdict (supersedes "thesis proven"):** lock-in escaped + cost supported for light AND heavy work (<$0.30/PR even with amend rounds); quality is conditional — heavy work needs the strong reviewer + amend cycle, so AGENT-8 Option B (the amend loop) is load-bearing, not optional. Untested: hardest/ambiguous reasoning + chief-tier (strong by design). Greenlight the port with the amend cycle load-bearing; registry persistence architecture (above-OpenCode layering, SQLite) → chief ADR. PR #28 informative, not ratifying.

## 2026-06-07 — dev-companion (P1 spine: AGENT-18, the real dispatch registry)

Started the P1 substrate spine (issues #32/#33/#34, strict dependency chain). Built **AGENT-18** inline → **PR #41**: `src/substrate/registry.ts` + 21-test co-located suite, the durable dispatch store + cheap-able-fraction instrument (ADR 0009). Ported properly from the informative spike (#28, closed), both of its self-review findings closed: setSessions wraps its two-id write in a transaction; terminal/non-terminal **derived** from the transition graph (`isTerminal` = no outgoing edges), never hardcoded. Extended the spike's 5-state machine to ADR 0009's full graph (`queued→building→review→amending→done` + `escalated`/`failed`, with building/review/amending→escalated|failed) and added the instrument columns (route, amend_rounds, escalated kind, per-leg cost build/review/amend). Upgrades over the spike: a typed `Dispatch` model + `rowToDispatch` boundary mapper that defaults every column (no bare `as` over raw rows, per standards § Typed boundaries); read-validate-write inside one txn for transition/escalate (closes a TOCTOU vs the skill example). **Convention change (owner-directed):** retired `.orchestrator/` as the *substrate* db home — that dir is the Claude-Code orchestrator's own ledger/cycle state; the registry db now defaults to `.substrate/` (mirrors `src/substrate/`), gitignored w/ WAL/SHM sidecars; ADR 0009 + persistence-bun-sqlite skill updated. typecheck clean, 26/26 tests. No repo CI configured (vacuous "green"); local gates are the bar. Awaiting owner merge of #41 before AGENT-19 (the loop daemon, which rewrites `src/dispatch/loop.ts` wholesale and clears the one stray `.orchestrator/` string left there).

## 2026-06-07 — dev-companion (AGENT-18, Drizzle pivot — architecture turn on #41)

Owner reviewed #41's first cut and made two calls that supersede the raw-SQL build: (1) published types belong in their own contract file, not co-located; (2) "I strongly detest raw SQL strings" + "used to node ORMs" → adopt an ORM. Surfaced that raw SQL was *prescribed* by ADR 0009 + the skill, so this is an architecture decision, not a touch-up; walked the Kysely-vs-Drizzle-vs-no-dep fork. Owner chose **Drizzle** and asked to bake the conventions into skills/ADRs. Reworked #41 (same branch, pre-merge): added `drizzle-orm@0.45.2` + `drizzle-kit@0.31.10`; split `src/substrate/dispatch.ts` (the contract — state machine + types) from `schema.ts` (the Drizzle table, schema-as-code) and `registry.ts` (behavior, typed query builder, `migrate()` at startup); generated + committed the initial migration (`drizzle/`). The typed-boundary smell is now *dissolved* — `InferSelectModel` gives the camelCase model directly, no row mapper, no `as`. The two spike findings still hold (transition graph derives terminal set; setSessions is one atomic UPDATE). **Conventions baked:** ADR 0016 (Drizzle, supersedes 0009 on mechanism only); `standards.md` (dependency carve-out, type-placement rule, a new § Persistence); skill renamed `persistence-bun-sqlite` → `persistence-drizzle` and rewritten; refs updated in CLAUDE.md, skills/README, adding-a-substrate-module. 21 registry tests unchanged + green on Drizzle; typecheck clean. #41 updated, still awaiting owner merge.

## 2026-06-07 — dev-companion (AGENT-18, "sloppy PR" — layered-context refactor)

Owner reviewed #41 again: CHANGES_REQUESTED + "Sloppy PR." Real findings — (1) architecture: are we following router/service/repository/engine? are registry/dispatch a subdir or is that what substrate is? (2) inline: transition/escalate unreadable + duplicated error handling, ORM chains verbose, constants placement. My "no blockers, ready for merge" self-review had missed all of it. Confirmed direction with the owner (subdir + repository rename + document the layering), then refactored #41 in one pass: the flat `src/substrate/{registry,dispatch,schema}.ts` became the **dispatch bounded context** `src/substrate/dispatch/{model,schema,repository}.ts` + `index.ts`, with an explicit **engine/repository/service/router** layering. `DispatchRegistry` → `DispatchRepository`. Deduped transition/escalate into one private `move()` (existence+legality checks inside the txn, unified `illegal transition X → Y` message); added `selectAll()`/`patch()` helpers to kill the repeated Drizzle chains; grouped config constants. Pushed back (with reasoning) on moving `NEWEST_FIRST` into the pure-domain model — it imports the schema, so it'd drag the ORM into the engine layer. Baked the layering into standards.md (new § + "SQL lives only in a repository" rule + module map), ADR 0016, persistence-drizzle skill. Migration unchanged (no drift); typecheck clean, 26/26. Lesson for #33/#34: build the loop + amend cycle AS the service layer (consume the repository, zero SQL), and don't rubber-stamp my own self-reviews. Awaiting owner re-review/merge.
