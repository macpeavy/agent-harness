{
  "name": "agent-harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Open, self-hosted runtime for an autonomous multi-agent software-engineering fleet (OpenCode + LiteLLM).",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "fanout": "bun run src/fanout-stress.ts",
    "daemon": "bun run src/dispatch/daemon.ts",
    "session-loop": "bun run src/dispatch/session-loop.ts",
    "reap": "bun run src/cli/reap.ts",
    "substrate": "bun run src/index.ts",
    "db:generate": "drizzle-kit generate"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "drizzle-kit": "^0.31.10",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "drizzle-orm": "^0.45.2",
    "zod": "^4.4.3"
  }
}
