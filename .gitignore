# Dependencies
node_modules/
.venv/
__pycache__/

# Secrets / environment — NEVER commit
.env
.env.local
*.local
mcp-auth.json

# Build output
dist/
build/
*.tsbuildinfo

# Logs
*.log
logs/

# Local runtime state (OpenCode sessions/auth, LiteLLM db) — not committed
.opencode/
*.db
litellm.db

# Substrate runtime state (dispatch registry db + WAL/SHM sidecars) — not committed
.substrate/

# Per-dispatch git worktrees — in-project so agent edits need no external_directory prompt (AGENT-38)
.worktrees/

# OS
.DS_Store
Thumbs.db
